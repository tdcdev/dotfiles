# -------------------- umask
umask 022

# -------------------- export environment variables
export PATH=/$HOME/.local/bin/:$PATH
export PATH=/opt/$USER/llvm/bin/:$PATH

# -------------------- virtualenvwrapper
export WORKON_HOME=~/.virtualenvs
mkdir -p $WORKON_HOME
source ~/.local/bin/virtualenvwrapper.sh
